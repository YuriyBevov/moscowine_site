mixin outlined-btn(cl, iconName, btnText, dataModal)
  a(href="#" class=cl ? cl : null data-modal= dataModal ? dataModal : null).outlined-btn
    svg(width="16" height="16")
      use(xlink:href="#" + iconName)
    span= btnText